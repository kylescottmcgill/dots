#!/bin/bash

if [[ -n "$*" ]]; then
    FILENAME="$*"
else
    FILENAME="$(date +'%s')_$HOSTNAME.png"
fi

scrot -m -q 80 ~/pictures/shots/${FILENAME} -e 'scp $f  turtil:/srv/vhosts/shots.turtil.net/public'
URI="http://shots.turtil.net/${FILENAME}"
echo -n $URI | tee >(xclip) && echo
